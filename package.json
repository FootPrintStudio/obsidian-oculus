{
	"name": "obsidian-media-gallery",
	"version": "1.5.0",
	"description": "Oculus — local and remote media galleries for Obsidian with grid, carousel, masonry, and a note-image lightbox.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"keywords": ["obsidian", "plugin", "gallery", "media"],
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.15.17",
		"esbuild": "0.25.5",
		"obsidian": "latest",
		"typescript": "^5.8.3"
	}
}
